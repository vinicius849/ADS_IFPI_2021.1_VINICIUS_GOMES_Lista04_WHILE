const prompt = require('prompt-sync')()

function main(){
    let num_1 = Number(input('Informe um valor para se dividir: '));
    let num_2 = Number(input('Informe aqui um divisor: '))
}

    divisao(num_1, num_2){
        let aux = n1;
        let r = 0;
        let resto;

        while(aux !== 0){ // Rever
            if (aux >= n2){
                r++;
                resto = aux;
            }else{
                aux = aux - aux
            }
        }
    console.log(`O quociente: ${r}/nResto: ${resto}`);
    }

main()


